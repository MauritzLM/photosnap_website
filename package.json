{
  "name": "photosnap-website",
  "version": "1.0.0",
  "description": "![Design preview for the Photosnap Website coding challenge](./preview.jpg)",
  "main": "index.js",
  "scripts": {
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "build:sass": "sass --style=compressed src/assets/sass:./public/css",
    "watch:sass": "sass --watch src/assets/sass:./public/css",
    "start": "npm-run-all --parallel watch:*",
    "build": "npm-run-all --parallel build:*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.77.2"
  }
}
